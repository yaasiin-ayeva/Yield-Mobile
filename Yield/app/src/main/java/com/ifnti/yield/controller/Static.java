package com.ifnti.yield.controller;

public class Static {
    //public static ArrayList<Sending> sendings = new ArrayList<>();

    public static void init() {
        /*Sending sending1 = new Sending();
        Sending sending2 = new Sending();
        Sending sending3 = new Sending();
        Sending sending4 = new Sending();

        sending1.setTextMessage("Yield, a future-oriented world for you!");
        sending1.setPhoneNo("98747193");

        sending2.setTextMessage("Yield, a new world is calling you!");
        sending2.setPhoneNo("90410204");

        sending3.setTextMessage("Yield, a new world is calling you!");
        sending3.setPhoneNo("93121871");

        sending4.setTextMessage("Yield, a future-oriented world for you!");
        sending4.setPhoneNo("70609243");

        sendings.add(sending1);
        sendings.add(sending2);
        sendings.add(sending3);
        sendings.add(sending4);
*/
    }
}
